# Entry point for Azure App Service
import os
import sys

# Add the main directory to the path
sys.path.append("main/grok")

# Import the FastAPI app
from vicky_app import app

# This variable is needed for Azure to find your app
app = app